.credential
  %aside
    .time-span #{year_month(edu.start_date)}&nbsp;&mdash;&nbsp;#{date_or_present(edu.end_date)}
    .job-location #{edu.location}
  .job-title= edu.position
  .job-organization= edu.organization.name
