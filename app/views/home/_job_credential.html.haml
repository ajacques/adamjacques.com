= render :layout => 'home/credential', :locals => {:credential => job, :position => job.position } do
  - if job.responsibilities != nil then
    %ul
      - job.responsibilities.each do |point|
        %li= point.text
